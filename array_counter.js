/**
 * Return an array with values that when imploded,
 *   equate to the imploded input value + 1
 *
 * example:
 *   inputArray = [8,0,0,1]
 *   return [8,0,0,2]
 *
 * Each element in the array must be a single digit Number
 *
 * @param  {Number} inputArray
 * @return {Number} <Array>
 */
function arrayCounter( inputArray ) {

  // do your work here

}















// export the function to be imported and tested by other modules
module.exports = arrayCounter;
